## 远程服务器系统监控采集方案

### 📋 实施方案概览

通过GitHub同步代码到远程服务器，使用Shell脚本调用Python脚本采集系统指标并存储到MySQL数据库。

### 🔧 实施步骤

#### 1️⃣ 更新Python采集脚本 (`monitor_collector.py`)
- 更新数据库密码为 `!A33b3e561fec`
- 移除对systemd服务的依赖
- 优化代码结构和错误处理

#### 2️⃣ 创建Shell启动脚本 (`run_monitor.sh`)
- 创建虚拟环境并安装依赖
- 提供启动、停止、日志查看功能
- 支持后台运行和日志轮转

#### 3️⃣ 创建数据库初始化脚本 (`init_db.sql`)
- 创建监控数据库和表结构
- 设置监控专用用户权限

#### 4️⃣ 同步到GitHub仓库
- 提交所有文件到GitHub
- 确保远程服务器可以拉取最新代码

#### 5️⃣ 远程服务器部署
- SSH连接到远程服务器
- 克隆GitHub仓库
- 初始化数据库
- 安装依赖并运行采集脚本

### 📁 生成文件清单
| 文件 | 说明 |
|------|------|
| `monitor_collector.py` | 系统指标采集Python脚本 |
| `run_monitor.sh` | Shell启动脚本 |
| `init_db.sql` | 数据库初始化SQL脚本 |
| `requirements.txt` | Python依赖列表 |

### ⏱️ 预计完成时间
约15-20分钟（不含实际运行时间）